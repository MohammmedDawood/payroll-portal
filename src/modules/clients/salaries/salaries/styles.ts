import styled from "styled-components";

export const SalariesWrapper = styled.div`
  display: grid;
  grid-template-columns: 100%;
  grid-template-rows: auto 1fr;
  gap: 16px;
  .input-root {
    max-width: 8rem;
  }
`;
